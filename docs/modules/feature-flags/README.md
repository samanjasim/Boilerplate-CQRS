# Feature Flags Module

Per-tenant feature flag overrides with platform-wide defaults, opt-out, and enforcement gates.

## Docs

- **[Developer Guide](developer-guide.md)** — defining flags, checking at runtime, tenant overrides.
